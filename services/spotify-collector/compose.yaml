services:
  spotify-collector:
    build: .
    env_file: .env
    user: "1000:1000"
    volumes:
      - ./data:/data
    restart: unless-stopped
    logging:
      driver: json-file
      options: { max-size: "10m", max-file: "3" }
